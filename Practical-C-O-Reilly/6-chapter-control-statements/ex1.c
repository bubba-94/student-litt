#include <stdio.h>
#include <math.h>

int main (){

    int a = 100;
    int b = 15;
    double square = sqrt(a - b);

    printf ("Sqaure root between point a: %i and b: %i = %f \n", a,b,square);
    return 0;
}